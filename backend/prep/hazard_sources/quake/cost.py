"""地域危険度ランク → 距離に掛ける係数。"""

from prep.paths import quake_gpkg

# ---------------- 地震ハザード（SPEC_C C-2） ----------------
# 東京都 第9回 地域危険度測定調査。公式SHPからbuild.pyで正規化したGPKGを使う
# （5,192町丁目 / 51市区町村 / EPSG:4326）。
QUAKE_SOURCE_ID = "risk9"
QUAKE_SOURCE_LABEL = "東京都 第9回地域危険度測定調査"
QUAKE_GPKG = quake_gpkg()

# 地域危険度ランク(1〜5) -> 距離に掛ける係数（SPEC_C C-3 の初期値）。**仮の値**。
QUAKE_COST = {1: 1.0, 2: 1.2, 3: 1.5, 4: 2.0, 5: 3.0}
# 町丁目ポリゴンの外（＝地震危険度が評価されていない）に与える係数。
# 浸水側の COVERAGE_PENALTY と同じ思想: 未評価を最安値にしない。
QUAKE_COVERAGE_PENALTY = 1.2

# ⚠️ 浸水深(m)と危険度ランクは**単位が違うので1つの数値には統合しない**（SPEC_C C-3）。
# 合成はコスト関数のレベルだけで行う:
#     edge_cost = flood_cost(depth) * quake_cost(rank)
# 掛け算にする理由は粒度差。地域危険度は町丁目単位(数百m)で、浸水深グリッド(9m)より
# 2桁粗い。加算だと粗いデータが細かいデータを押しつぶすが、乗算なら
# 「浸水の細かい差が、危険度の高い地域では増幅される」という素直な挙動になる。

# SPEC 3「北千住↔上野 区間の分布（実測）」の再現用範囲
SPEC_STATS_BBOX = (35.705, 35.760, 139.770, 139.812)  # lat0, lat1, lon0, lon1
SPEC_STATS_EXPECTED = {
    "n_points": 181613,
    ">0.0": 75.2,
    ">0.2": 53.0,
    ">0.3": 45.1,
    ">0.5": 31.6,
    ">1.0": 3.6,
    ">2.0": 0.1,
}
# --------------------------------------
